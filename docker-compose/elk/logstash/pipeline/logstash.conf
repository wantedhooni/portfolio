input {
  tcp {
    port => 4560
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{[service]}-%{[env]}-%{+YYYY.MM.dd}"
    action => "create"
  }
  stdout { codec => rubydebug }
}
