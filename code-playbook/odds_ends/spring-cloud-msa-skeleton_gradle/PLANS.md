# 작업 계획

## Gradle Groovy DSL 전환

- [x] 루트 및 하위 모듈의 Gradle 빌드 파일 현황을 확인한다.
- [ ] 루트 `build.gradle`의 Kotlin DSL 표현을 Groovy DSL로 변경한다.
- [ ] 하위 모듈의 `build.gradle.kts`를 Groovy DSL 기반 `build.gradle`로 변경한다.
- [ ] Gradle 명령으로 전체 프로젝트 구성을 검증한다.
