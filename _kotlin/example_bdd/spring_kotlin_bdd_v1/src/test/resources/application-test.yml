spring:
  jpa:
    hibernate:
      ddl-auto: create-drop                            # 테스트마다 스키마 재생성
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100