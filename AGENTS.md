# 작업 지침

## 기본 원칙
- 현재 작업공간인 `securities_monolithic` 내부만 대상으로 작업한다.
- 실무 수준의 구조와 유지보수성을 우선으로 판단한다.
- UI/UX 개선 시에도 기존 기능을 깨지 않는 방향으로 수정한다.

## 문서화 원칙
- 계획은 `PLANS.md`에 한글로 정리한다.
- 현재 작업 내용은 `TASK.md`에 한글로 기록한다.
- 작업 완료 후 변경 사항에 맞춰 `README.md`를 갱신한다.

## 구현 원칙
- 백엔드의 `class`, `service`, `service method`에는 한글 doc 주석을 유지한다.
- 프로젝트 루트의 `script` 폴더에서 전체 기동/중지/재시작 스크립트를 관리한다.
- `all-start.sh`, `all-restart.sh` 실행 후 접속 URL과 데모 계정 정보를 출력한다.
