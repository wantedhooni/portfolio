input {
  tcp {
    port => 4560
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{[APP_NAME]}-%{[ENV]}-%{+YYYY.MM.dd}"
    action => "create"
  }
  stdout { codec => rubydebug }
}
